import tw, {theme} from 'twin.macro'
import {styled} from '../stitches.config'
import useStore from '../store'


const Window = ({mesh, title, children, onDrag, onClose}) => {
  return (
  )
}

export default Window